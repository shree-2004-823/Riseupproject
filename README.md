
  # Start app development

  This is a code bundle for Start app development. The original project is available at https://www.figma.com/design/gSlB57V3oTakHnViV67hDf/Start-app-development.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  